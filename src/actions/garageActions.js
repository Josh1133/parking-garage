//gets the vacancy data from the 'database' and returns the raw results
//no paramaters
export function isVacancy() {
  const vacancy = {
    totalSpots: 100,
    totalSpotsTaken: 95
  };

  return vacancy;
}
